@{
    Dictionary<string, object> filters = new Dictionary<string, object>()
    {
        { SiteGlobals.DATE_RANGE_FILTER_NAME, DateTime.Now }
    };
    ViewData["Title"] = @Html.GetContentAsString(SiteGlobals.PRIVACY_REPOSITORY_NAME, "Title", filters);
}
<h1>@Html.GetContentAsString(SiteGlobals.PRIVACY_REPOSITORY_NAME, "Title", filters)</h1>

<p>@Html.GetContentAsString(SiteGlobals.PRIVACY_REPOSITORY_NAME, "Content", filters)</p>
